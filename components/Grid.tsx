import styled from 'styled-components';

const Box = styled.div`
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  height: 100%;
  position: relative;
  overflow: hidden;
`;

export default Box;
